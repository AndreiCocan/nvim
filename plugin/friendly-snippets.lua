-- VSCode-style snippet collection. No setup() — it's just snippet data that
-- blink.cmp auto-detects on the runtimepath via its built-in `snippets` source.
vim.pack.add {
  'https://github.com/rafamadriz/friendly-snippets',
}
